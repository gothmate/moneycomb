import styled from "styled-components";

export const Container = styled.main`
  max-width: 70%;
  margin: 0 auto;
  padding: 1.5rem 1rem;
  display: flex;
  gap: 1rem;
  justify-content: space-around;
`